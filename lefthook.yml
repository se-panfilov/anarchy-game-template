pre-commit:
  parallel: true
  commands:
    prettier:
      glob: '*.{ts,tsx,js,jsx,json,css,scss,sass,txt,yaml,md}'
      run: npm run pretty-quick
    gitleaks:
      run: gitleaks protect --staged --verbose
